[workspace]
resolver = "2"
members = ["crates/gpbeam-core", "crates/gpbeam-cli"]
# src-tauri is added in Phase 9 (it has its own build flow)

[workspace.package]
edition = "2021"
version = "0.1.0"
license = "MIT"
